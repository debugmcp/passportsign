{
  "name": "@passportsign/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared binding state machine, canonical serialization, in-toto statements, bundles, GitHub gist check, zkPassport SDK wrapper, Rekor client, SQLite cache, and offline verifier for passportsign.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@truestamp/canonify": "1.0.3",
    "better-sqlite3": "^11.6.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12"
  }
}
